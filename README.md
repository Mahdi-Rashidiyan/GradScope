# GradScope
Most training bugs are invisible. Your loss goes down. Your accuracy looks fine. But your model is stuck in a bad basin of attraction — locked there by gradient norm imbalance, directional instability, or a curriculum that's destroying generalization.GradScope makes these dynamics visible in real time.
